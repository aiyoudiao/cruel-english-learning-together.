# 扩展与改进建议 (Roadmap)

## 短期改进
1. **输入验证**：增强前端对输入数据的验证。
2. **性能优化**：优化图片上传（并发上传）。
3. **缓存**：实现浏览器端数据缓存，减少 API 请求。

## 长期改进
1. **后端 API**：实现独立的后端服务，解耦 GitHub API。
2. **用户认证**：集成 GitHub OAuth 登录。
3. **通知功能**：打卡提醒和周报推送。
4. **数据导出**：支持导出打卡记录为 Markdown 或 PDF。
